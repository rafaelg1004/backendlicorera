const BaseController = require('./BaseController');

class ListaPreciosController extends BaseController {
  constructor(service) {
    super(service);
  }
}

module.exports = ListaPreciosController;
