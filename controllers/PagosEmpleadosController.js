const BaseController = require('./BaseController');

class PagosEmpleadosController extends BaseController {
  constructor(service) {
    super(service);
  }
}

module.exports = PagosEmpleadosController;
